<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".WallCrawlApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WallCrawl">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.WallCrawl"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Stores the chosen app language on Android 12 and lower, where the platform has
            no per-app language store of its own. Android 13+ ignores this and keeps the
            choice in the system, which is what keeps the in-app selector and the system
            per-app language screen showing the same value.

            The service is declared disabled on purpose: AndroidX only reads this metadata,
            it never starts the component. The preference lands in SharedPreferences, which
            backup_rules.xml and data_extraction_rules.xml already exclude, so a language
            choice is device-local and is never transferred off the device.
        -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>
    </application>

</manifest>
